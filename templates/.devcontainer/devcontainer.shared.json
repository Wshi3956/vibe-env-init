{
  "name": "{{PROJECT_NAME}}",
  "image": "miskamyasa/mise-ub-24-04:3",
  "remoteUser": "ubuntu",
  "workspaceFolder": "${localWorkspaceFolder}",
  "workspaceMount": "source=${localWorkspaceFolder},target=${localWorkspaceFolder},type=bind",
  "containerEnv": {
    "MISE_TRUSTED_CONFIG_PATHS": "${localWorkspaceFolder}",
    "OPENCODE_YOLO": true
  },
  "mounts": [
    "source=${localEnv:HOME}/.cache/containers/cache,target=/home/ubuntu/.cache,type=bind",
    "source=${localEnv:HOME}/.cache/containers/local-share,target=/home/ubuntu/.local/share,type=bind",
    // In this configuration, we share opencode data folder to enable sessions sharing between different containers
    "source=${localEnv:HOME}/.local/share/opencode,target=/home/ubuntu/.local/share/opencode,type=bind"
  ],
  "runArgs": [
    "--name={{PROJECT_NAME}}"
  ],
  "postCreateCommand": "mise install"
}
